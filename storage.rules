rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Storage is not used by the current build. Deny everything until a feature needs it.
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
